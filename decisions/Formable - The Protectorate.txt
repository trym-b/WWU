country_decisions = {
    form_the_protectorate = {
		major = yes
        ai_importance = 300
        
		potential = { 
            NOT = { has_country_flag = enacted_final_tag_formation }
            
            # Exclusions
			NOT = { exists = PRT }
            
            # Requisites
			tag = P18
			exists = F80
		}
        
		allow = {
            # General
			is_at_war = no	
			stability = 1
			total_development = 100
            
			# Cost
            adm_power = 100
			dip_power = 100
			mil_power = 100
		}
        
		effect = {
            # Cost
			add_adm_power = -100
			add_dip_power = -100
            add_mil_power = -100
            
            # Switch
			change_tag = PRT
			set_government_rank = 2
            
			# Bonus
            add_formable_bonus = yes
            #trigger_ni_switch = yes
            #trigger_mission_switch = yes
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}