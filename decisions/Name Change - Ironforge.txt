country_decisions = {
    form_fel_vindicaar = {
        ai_importance = 0
        
        potential = {
            tag = A29
            NOT = { has_country_flag = is_ironforge }
        }

        allow = {
            adm_power = 10
            dip_power = 10
            area_ironforge = {
                owned_by = ROOT
            }
        }
        effect = {
            add_adm_power = -10
            add_dip_power = -10
            override_country_name = IRONFORGE
            hidden_effect = {
                set_country_flag = is_ironforge
            }
        }
        ai_will_do = {
            factor = 0
        }
    }
}