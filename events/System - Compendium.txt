namespace = wwu_compendium

# # Ruler
# country_event = {
	# id = wwu_compendium.1
	# title = wwu_compendium.1.title
	# desc = wwu_compendium.1.desc
	# picture = RULER_VIEW_eventPicture
	
	# is_triggered_only = yes
    
    # # Invest in Ruler
    # option = {
		# name = wwu_compendium.1.option.ruler_investment
        
        # country_event = { id = wwu_ruler_investment.10 }
        
        # hidden_effect = {
            # if = {
                # limit = {
                    # ai = yes
                # }
                # add_country_modifier = {
                    # name = ai_ruler_investment_look_cooldown
                    # duration = 365
                    # hidden = yes
                # }
            # }
        # }
        
        # ai_chance = {
            # factor = 1
            
            # modifier = {
                # factor = 0
                
                # NOT = { adm_power = 800 }
                # NOT = { dip_power = 800 }
                # NOT = { mil_power = 800 }
            # }
            
            # modifier = {
                # factor = 0
                
                # has_country_modifier = ai_ruler_investment_look_cooldown
            # }
        # }
	# }
    
    # # Retire [Root.Monarch.GetName]
    # option = {
		# name = wwu_compendium.1.option.retire
        
        # trigger = {
            # months_of_ruling = 64 # Must have ruled for 5 years minimum. Prevents exploitative ruler cycling
        # }
        
        # kill_ruler = yes
        
        # if = {
            # limit = {
                # NOT = { legitimacy_equivalent = 50 }
            # }
            # add_stability = 1
        # }
        
        # ai_chance = {
            # factor = 0
        # }
	# }
    
    # option = {
		# name = wwu_compendium.1.option.exit
        
        # ai_chance = {
            # factor = 1
        # }
	# }
# }

# Realm
country_event = {
	id = wwu_compendium.2
	title = wwu_compendium.2.title
	desc = wwu_compendium.2.desc
	picture = HALLS_OF_STONE_eventPicture
	
	is_triggered_only = yes
    
    # View Artifacts
    option = {
		name = wwu_compendium.2.option.view_artifacts
        
        trigger = {
            has_global_flag = enabled_archaeology_mechanics
        }
        
        country_event = { id = wwu_artifact_repository.1 }
        
        ai_chance = {
            factor = 0
        }
	}
}

# Game
country_event = {
	id = wwu_compendium.3
	title = wwu_compendium.3.title
	desc = wwu_compendium.3.desc
	picture = TIME_WARP_eventPicture
	
	is_triggered_only = yes
    
    trigger = {
        ai = no
    }
    
    # View Game Configuration
    option = {
		name = wwu_compendium.3.option.game_configuration
        
        country_event = { id = wwu_game_configuration.1 }
        
        ai_chance = {
            factor = 0
        }
	}
    
    # View Wiki
    # option = {
		# name = wwu_compendium.3.option.wiki
        
        # country_event = { id = wwu_wiki.1 }
        
        # ai_chance = {
            # factor = 0
        # }
	# }
    
    option = {
		name = wwu_compendium.2.option.exit
        
        ai_chance = {
            factor = 1
        }
	}
}
