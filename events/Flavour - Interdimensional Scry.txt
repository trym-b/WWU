namespace = wwu_interdimensional_scry

# Scry into Deepholm
country_event = {
	id = wwu_interdimensional_scry.1
	title = wwu_interdimensional_scry.1.title
	desc = wwu_interdimensional_scry.1.desc
	picture = EARTH_ELEMENTAL_eventPicture

	trigger = {
        NOT = { has_country_flag = scry_into_deepholm }
	}
	
	mean_time_to_happen = { 
        years = 50
    }

	option = {
		name = wwu_interdimensional_scry.1.option.a
		
        
    }
    option = {
		name = wwu_interdimensional_scry.1.option.b
		
        
    }
}
