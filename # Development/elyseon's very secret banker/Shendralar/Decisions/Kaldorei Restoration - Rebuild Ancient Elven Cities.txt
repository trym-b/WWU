#Rebuild Eldarath
country_decisions = {
    decision_rebuild_eldarath = {
		major = no
        ai_importance = 0
        
		potential = {        
			NOT = { tag = ELD }
			NOT = { exists = ELD }
			has_country_flag = kaldorei_restoration_active
		}
		
		allow = {
            treasury = 1000
			stability = 3
		}
		
		effect = {
            # Cost
			add_treasury = -1000
			add_prestige = 20
        
            country_event = {
				id = flavor_decision_reb_anc_elf_cities.1			
            }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}

#Rebuild Eldre'thar
country_decisions = {
    decision_rebuild_eldrethar = {
		major = no
        ai_importance = 0
        
		potential = {        
			NOT = { tag = ELE }
			NOT = { exists = ELE }
			has_country_flag = kaldorei_restoration_active
		}
		
		allow = {
            treasury = 1000
			stability = 3
		}
		
		effect = {
            # Cost
			add_treasury = -1000
			add_prestige = 20
        
            country_event = {
				id = flavor_decision_reb_anc_elf_cities.2			
            }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}

#Rebuild Lathar'lazar
country_decisions = {
    decision_rebuild_latharlazar = {
		major = no
        ai_importance = 0
        
		potential = {        
			NOT = { tag = LAT }
			NOT = { exists = LAT }
			has_country_flag = kaldorei_restoration_active
		}
		
		allow = {
            treasury = 1000
			stability = 3
		}
		
		effect = {
            # Cost
			add_treasury = -1000
			add_prestige = 20
        
            country_event = {
				id = flavor_decision_reb_anc_elf_cities.3			
            }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}

#Rebuild Loreth'aran
country_decisions = {
    decision_rebuild_loretharan = {
		major = no
        ai_importance = 0
        
		potential = {        
			NOT = { tag = LOR }
			NOT = { exists = LOR }
			has_country_flag = kaldorei_restoration_active
		}
		
		allow = {
            treasury = 1000
			stability = 3
		}
		
		effect = {
            # Cost
			add_treasury = -1000
			add_prestige = 20
        
            country_event = {
				id = flavor_decision_reb_anc_elf_cities.4			
            }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}

#Rebuild Morlos'aran
country_decisions = {
    decision_rebuild_morlosaran = {
		major = no
        ai_importance = 0
        
		potential = {        
			NOT = { tag = MOR }
			NOT = { exists = MOR }
			has_country_flag = kaldorei_restoration_active
		}
		
		allow = {
            treasury = 1000
			stability = 3
		}
		
		effect = {
            # Cost
			add_treasury = -1000
			add_prestige = 20
        
            country_event = {
				id = flavor_decision_reb_anc_elf_cities.5			
            }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}

#Rebuild Eldre'thalas
country_decisions = {
    decision_rebuild_eldrethalas = {
		major = no
        ai_importance = 0
        
		potential = {        
			NOT = { tag = ELR }
			NOT = { exists = ELR }
			has_country_flag = kaldorei_restoration_active
		}
		
		allow = {
            treasury = 1000
			stability = 3
		}
		
		effect = {
            # Cost
			add_treasury = -1000
			add_prestige = 20
        
            country_event = {
				id = flavor_decision_reb_anc_elf_cities.6			
            }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}

#Rebuild Isildien
country_decisions = {
    decision_rebuild_isildien = {
		major = no
        ai_importance = 0
        
		potential = {        
			NOT = { tag = ISI }
			NOT = { exists = ISI }
			has_country_flag = kaldorei_restoration_active
		}
		
		allow = {
            treasury = 1000
			stability = 3
		}
		
		effect = {
            # Cost
			add_treasury = -1000
			add_prestige = 20
        
            country_event = {
				id = flavor_decision_reb_anc_elf_cities.7			
            }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}

#Rebuild Kel'theril
country_decisions = {
    decision_rebuild_keltheril = {
		major = no
        ai_importance = 0
        
		potential = {        
			NOT = { tag = KEL }
			NOT = { exists = KEL }
			has_country_flag = kaldorei_restoration_active
		}
		
		allow = {
            treasury = 1000
			stability = 3
		}
		
		effect = {
            # Cost
			add_treasury = -1000
			add_prestige = 20
        
            country_event = {
				id = flavor_decision_reb_anc_elf_cities.8			
            }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}

#Rebuild Mathystra
country_decisions = {
    decision_rebuild_mathystra = {
		major = no
        ai_importance = 0
        
		potential = {        
			NOT = { tag = MAT }
			NOT = { exists = MAT }
			has_country_flag = kaldorei_restoration_active
		}
		
		allow = {
            treasury = 1000
			stability = 3
		}
		
		effect = {
            # Cost
			add_treasury = -1000
			add_prestige = 20
        
            country_event = {
				id = flavor_decision_reb_anc_elf_cities.9			
            }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}

#Rebuild Nar'thalas
country_decisions = {
    decision_rebuild_narthalas = {
		major = no
        ai_importance = 0
        
		potential = {        
			NOT = { tag = NAL }
			NOT = { exists = NAL }
			has_country_flag = kaldorei_restoration_active
		}
		
		allow = {
            treasury = 1000
			stability = 3
		}
		
		effect = {
            # Cost
			add_treasury = -1000
			add_prestige = 20
        
            country_event = {
				id = flavor_decision_reb_anc_elf_cities.10			
            }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}

#Rebuild Suramar
country_decisions = {
    decision_rebuild_suramar = {
		major = no
        ai_importance = 0
        
		potential = {        
			NOT = { tag = SUR }
			has_country_flag = kaldorei_restoration_active
		}
		
		allow = {
            treasury = 1000
			stability = 3
		}
		
		effect = {
            # Cost
			add_treasury = -1000
			add_prestige = 20
        
            country_event = {
				id = flavor_decision_reb_anc_elf_cities.11			
            }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}

#Rebuild Shandaral
country_decisions = {
    decision_rebuild_shandaral = {
		major = no
        ai_importance = 0
        
		potential = {        
			NOT = { tag = SHN }
			NOT = { exists = SHN }
			has_country_flag = kaldorei_restoration_active
		}
		
		allow = {
            treasury = 1000
			stability = 3
		}
		
		effect = {
            # Cost
			add_treasury = -1000
			add_prestige = 20
        
            country_event = {
				id = flavor_decision_reb_anc_elf_cities.12			
            }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}