#----------------------------------------
# Custom GUI - For non-specific elements
#----------------------------------------
#--------------
# Mission View
#--------------
# Titles
custom_text_box = {
   name = window_title
   potential = { 
        NOT = { is_in_mission_tab__rise_of_the_horde = yes }
   }
}
custom_text_box = {
   name = rise_of_the_horde_window_title
   potential = { 
        is_in_mission_tab__rise_of_the_horde = yes
   }
}


# Close
custom_button = {
    name = stratagem_close_button
    
    potential = {
        is_stratagem_gui_open = yes
    }
    
    trigger = { 
        always = yes
    }
    
    effect = { 
        close_stratagem_gui = yes
        close_archaeology_gui = yes
    }
}

# Close
custom_button = {
    name = archaeology_close_button
    
    potential = {
        is_archaeology_gui_open = yes
    }
    
    trigger = { 
        always = yes
    }
    
    effect = { 
        close_stratagem_gui = yes
        close_archaeology_gui = yes
    }
}

# Mission Window Toggle
custom_button = {
    name = mission_window_toggle_button
    tooltip = MISSION_BUTTON_HOVER_TT
    
    potential = {
        always = yes
    }
    
    trigger = { 
        always = yes
    }
    
    effect = { 
        close_stratagem_gui = yes
        close_archaeology_gui = yes
    }
}

# Archaeology
custom_icon = {
   name = archaeology_window_background
   potential = { 
        is_archaeology_gui_open = yes
   }
}

# Archaeology Window Toggle
custom_button = {
    name = archaeology_window_toggle_button
    tooltip = ARCHAEOLOGY_BUTTON_HOVER_TT
    
    potential = {
        always = yes
    }
    
    trigger = { 
        always = yes
    }
    
    effect = { 
        open_archaeology_gui = yes
        close_stratagem_gui = yes
    }
}

# Stratagem
custom_icon = {
   name = stratagem_window_background
   potential = { 
        is_stratagem_gui_open = yes
   }
}

# Stratagem Window Toggle
custom_button = {
    name = stratagem_window_toggle_button
    tooltip = STRATAGEM_BUTTON_HOVER_TT
    
    potential = {
        always = yes
    }
    
    trigger = { 
        always = yes
    }
    
    effect = { 
        open_stratagem_gui = yes
        close_archaeology_gui = yes
    }
}

#--------------
# Tabs
#--------------
custom_button = {
    name = personal_missions_tab
    tooltip = SHOW_PERSONAL_MISSIONS_TT
    
    potential = {
        # There share the core window type, hide the tab if within them
        NOT = { is_archaeology_gui_open = yes }
        NOT = { is_stratagem_gui_open = yes } 
        
        # Only show if they actually have an option
        has_mission_tab__rise_of_the_horde = yes
    }
    
    trigger = { 
        always = yes
    }
    
    effect = { 
        switch_missions_to_personal = yes
    }
}
custom_button = {
    name = story_missions_tab
    tooltip = SHOW_RISE_OF_THE_HORDE_TT
    
    potential = {
        # There share the core window type, hide the tab if within them
        NOT = { is_archaeology_gui_open = yes }
        NOT = { is_stratagem_gui_open = yes } 
        
        # Only show if they actually have an option
        has_mission_tab__rise_of_the_horde = yes
    }
    
    trigger = { 
        always = yes
    }
    
    effect = { 
        switch_missions_to_rise_of_the_horde_missions = yes
    }
}