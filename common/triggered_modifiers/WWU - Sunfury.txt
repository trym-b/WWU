terrifying_visage = {
    potential = {
		any_country = {
            war_with = ROOT
            has_country_flag = kiljaeden_summoned
            religion = fel
        }
        NOT = { religion = fel }
    }
	trigger = {
        NOT = { religion = fel }
	}
	
    land_morale = -0.2
    manpower_recovery_speed = -0.2
}
